spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/portfolioboard?characterEncoding=utf8&autoReconnect=true

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
#        show_sql: true
#        use_sql_comments: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace

file:
  dir:c:/Users/user/springFile/
